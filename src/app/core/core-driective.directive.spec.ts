import { CoreDriectiveDirective } from './core-driective.directive';

describe('CoreDriectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDriectiveDirective();
    expect(directive).toBeTruthy();
  });
});
